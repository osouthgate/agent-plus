# Local env / secrets
.env
.env.local
.env.*.local

# Python
__pycache__/
*.pyc
.venv/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
.gstack/
plans/*
!plans/backlog/
.claude/settings.local.json

# gstack auto-creates CLAUDE.md with skill routing; we use AGENTS.md instead
/CLAUDE.md

# agent-plus workspace cache (per-repo, populated by `agent-plus init/refresh`)
.agent-plus/
