# STAGING.md — private staging context (NOT released to public)

This file lives only in the **private** `osouthgate/plans-agent-plus` clone. It is intentionally absent from the public `osouthgate/agent-plus` repo. If you are reading this in the public repo, something went wrong with a cherry-pick — delete it and amend the commit.

## What this repo is

`plans-agent-plus` is the **private staging clone** of [`osouthgate/agent-plus`](https://github.com/osouthgate/agent-plus) — the framework-only repo. As of the 2026-04-28 framework extraction, agent-plus ships only the four universal primitives (`agent-plus` meta, `repo-analyze`, `diff-summary`, `skill-feedback`) plus `skill-plus` (in design). The 10 service wrappers have moved to a separate marketplace at `osouthgate/agent-plus-skills` (live, v0.2.0) and no longer stage through this repo.

All new framework work — primitive plugins, envelope-contract changes, doc rewrites, the marketplace install command — is built and tested here first. Once a slice is solid (tests green, drift hook clean, dogfooded for at least one session), it gets promoted to the public repo.

The public repo is the user-facing artifact. The private repo is the build surface.

## Branch model

- `main` — the integration branch in this private repo. Slices fast-forward onto it after they land.
- `slice-N-<topic>` — short-lived branches per discrete unit of work. One slice = one logical feature + matching docs/tests/changelog. Land via `git merge --ff-only` to private `main`, then `git push origin main`.
- The drift hook (`.claude/hooks/check-readme-drift.sh`) and the SessionStart context hook (`.claude/hooks/agent-plus-context.py`) are repo-resident and run automatically.

## Release / promotion ritual

When promoting a slice from private `main` to public `agent-plus/main`:

1. **Identify the commits to promote.** Usually a single commit per slice (the slice landed FF onto private main as one feature commit).
2. **Cherry-pick into a public branch:**
   ```bash
   cd C:/dev/agent-plus  # or a fresh clone
   git fetch
   git checkout main
   git checkout -b release/<slice-topic>
   # cherry-pick the SHA(s) from the private repo. The private repo is configured
   # locally as `upstream` on the public clone so it's just:
   git cherry-pick <sha>
   ```
3. **Verify there's no staging residue:** `STAGING.md` must NOT be in the cherry-picked diff. If it is, abort and remove from the diff.
4. **Run tests in the public clone**, push the branch, open a PR (or push to main directly if going solo).
5. **Run the AGENTS.md item #4 topic-add reminders** (see queue below) for any new plugin shipped in the slice.

Use `git format-patch <range>` + `git am` if a slice spans multiple commits and the cherry-pick range is awkward.

## Framework extraction (in progress)

The 2026-04-28 split moves agent-plus to framework-only. Pre-extraction snapshot is preserved at `C:/dev/plans-agent-plus-archive/` for reference during migration.

The 10 wrappers — `github-remote`, `linear-remote`, `vercel-remote`, `supabase-remote`, `railway-ops`, `openrouter-remote`, `langfuse-remote`, `hermes-remote`, `coolify-remote`, `hcloud-remote` — are migrating to `osouthgate/agent-plus-skills` per [`plans/todo/2026-04-28-framework-extraction.md`](./plans/todo/2026-04-28-framework-extraction.md). Once that repo exists they ship from there independently.

**Wrapper staging discipline is being dropped.** Pre-1.0, the wrappers are stable enough that the cherry-pick-from-private overhead isn't justified — and once they leave this repo, there's no shared `main` to stage against anyway. They'll iterate directly on `osouthgate/agent-plus-skills`.

**Framework-plugin staging discipline continues unchanged.** The four universal primitives plus `skill-plus` still go through the private→public cherry-pick ritual described above.

## Pending public-release queue

Things to do the next time we promote slices to public. Update this list as slices land.

### Topic adds (per AGENTS.md item #4)

These are GitHub topic adds that can only happen on the public repo (auth-gated):

- [ ] `gh repo edit osouthgate/agent-plus --add-topic agent-plus`
- [ ] `gh repo edit osouthgate/agent-plus --add-topic repo-analyze`
- [ ] `gh repo edit osouthgate/agent-plus --add-topic diff-summary` *(after slice 5 lands)*
- [ ] `gh repo edit osouthgate/agent-plus --add-topic skill-plus` *(after slice A lands)*

Wrapper-related topic adds (`github-remote`, `vercel-remote`, etc.) are no longer queued here — those wrappers are migrating to `osouthgate/agent-plus-skills` and their topic adds happen on that repo instead. When `osouthgate/agent-plus-skills` is created, run `gh repo edit osouthgate/agent-plus-skills --add-topic agent-plus-skills` for marketplace discoverability.

### Slices ready to promote

| Slice | Private SHA | Plugin / change | Public-side notes |
|---|---|---|---|

(Empty — promotion queue cleared 2026-04-28 evening. All previously-queued slices either landed on public via the v0.9.0 promotion (slices 1–5, 8, MP1, MP2 — all bundled into public commits up through `db7ff68`) or are private-only (slices 6 + 7). The next promotable slice will be the gate-2 papercut bundle in `plans/todo/2026-04-28-unified-plan.md`.)

### Public release log

| Tag | Public SHA | Released | Contents |
|---|---|---|---|
| v0.1.0 | (early) | pre-2026-04 | Initial framework split |
| v0.2.0 | `c923160` | 2026-04-28 | Status README + framework-only restructure |
| v0.9.0 | `db7ff68` | 2026-04-28 | Marketplace install/update/list/remove + trust model (Phase 2) |
| v0.9.1 | `abe11db` | 2026-04-30 | Papercut bundle: services.json cleanup, repo-analyze deque BFS, diff-summary public-API regex fix, slice-8 coverage |
| v0.10.0 | `6f24782` | 2026-04-30 | skill-plus 0.1.0 (the fifth universal primitive — scan/propose/install-cron/scaffold/list/feedback/promote, 83 tests). agent-plus marketplace `search` + collision-resolution `prefer` (Phase 4). `core.autocrlf=false` pinned by `agent-plus init`. Wrappers deprecated from this repo's marketplace.json. Root + agent-plus README rewrite. PR #9. Topic `skill-plus` added to public repo. |
| v0.10.1 | `5426207` | 2026-04-30 | README polish pass driven by re-audit (excitement 3→7): version drift normalised across 6 worked-example JSON blocks; sibling-nav strip on all 5 plugin READMEs; voice-consistency pass (skill-feedback 167→151, skill-plus 139→132, agent-plus 302→280); AGENTS.md `~500 lines` claim fixed. Cross-repo: agent-plus-skills wrappers aligned to rc=0 + `status: "unconfigured"` for refresh-handler contract parity. Extension-API canary design spec written (private). PR #10. |
| v0.11.0 | `dd347f6` | 2026-04-30 | **BREAKING.** Rename meta plugin: `agent-plus` → `agent-plus-meta`. Resolves the audit-flagged naming collision between the framework and its meta primitive. Migration: `claude plugin uninstall agent-plus@agent-plus && claude plugin install agent-plus-meta@agent-plus`; CLI `agent-plus init/...` → `agent-plus-meta init/...`. Framework name, repos, `.agent-plus/` workspace dir, `AGENT_PLUS_*` env vars all unchanged. Root README tail cut: contributor sections moved to new `CONTRIBUTING.md`. CI workflow fixed to iterate the renamed plugin set + add skill-plus (was missing) + install pytest. PR #11. |
| v0.11.1 | `5204631` | 2026-04-30 | Audit-3 polish: fix dead sibling-nav links (`../agent-plus` → `../agent-plus-meta`) introduced by the v0.11.0 rename across 4 plugin READMEs; remove rename-blockquote lede leak from `agent-plus-meta/README.md`; add "all five at once" install one-liner; add a "90-second tour" terminal-cast section above Install showing the framework's actual flow. PR #12. |
| v0.11.2 | `3e3c088` | 2026-04-30 | "What gets us better scores" polish: 6 shields.io badges row + TOC line + ASCII framework-vs-marketplace diagram + tightened pitch line on root README. agent-plus-meta README split 285→216 lines (Extensions + Resolution moved to `docs/`). New `agent-plus-meta doctor` subcommand (read-only diagnostic, verdict ladder broken/degraded/healthy). New `install.sh` POSIX one-shot installer at repo root (`curl -fsSL .../install.sh \| sh` for all 5 primitives, `AGENT_PLUS_INSTALL_DIR` override, `--dry-run`). +10 tests (5 doctor + 5 install). Audit trajectory: pitch 8→9, onboarding 7→9, voice 7→9, info-arch 7→9, trust 8→9. PR #13. |
| v0.11.3 | `d46f487` | 2026-04-30 | Animated tour GIF closes the audit's last open dimension (excitement 7→9). Synthetic asciinema v2 cast at `assets/tour.cast` (plain JSON, deterministic regenerator at `assets/generate_tour_cast.py`); rendered to `assets/tour.gif` (530KB, 940×649px) via `agg` (Rust binary on PATH or `ghcr.io/asciinema/agg` Docker image fallback). Build script `assets/build_tour_gif.sh` handles Git Bash path-mount quirk. Embedded under the pitch on the root README. CONTRIBUTING.md gains a "The tour GIF" section. PR #14. |
| v0.15.2 | `20bfe5e` | 2026-05-01 | **doctor primitives multi-source detection (P2 follow-up to v0.15.1's dogfood gate).** Final dogfood gate against v0.15.1 surfaced one cosmetic-but-misleading P2: `agent-plus-meta doctor` reported `verdict: degraded` on a perfectly correct fresh install when `$AGENT_PLUS_INSTALL_DIR` wasn't on the user's `$PATH` yet (the post-install footer prompts the user to add `~/.local/bin` to PATH; before they do, every `shutil.which("agent-plus-meta")` returns None even though the wrapper exists at `$INSTALL_DIR/agent-plus-meta` and the plugin tree exists at `$PREFIX/agent-plus-meta/`). Same applies to tempdir-overridden test runs and the CI dogfood gate itself. doctor was looking through the wrong window — `shutil.which` only sees PATH. **Fix**: doctor primitives detection is now multi-source, resolving through 3 paths in order with the source recorded per-primitive in a new envelope field `primitives_source`: (1) `shutil.which(prim)` → `"path"` (covers post-install + Claude-plugin-installed primitives); (2) `$AGENT_PLUS_INSTALL_DIR/<prim>` exists → `"install_dir"` (covers fresh install before user adds to PATH, tempdir tests, CI dogfood); (3) `$AGENT_PLUS_PREFIX/<prim>/.claude-plugin/plugin.json` is a file → `"prefix"` (covers v0.15.1 tarball-install tree). Otherwise → `"missing"`. The new `primitives_source` envelope field is additive — non-breaking. The `primitives` field's `installed | missing` enum is unchanged. Net effect: a fresh `curl|sh` install that hasn't yet had `$INSTALL_DIR` added to `$PATH` now correctly reports `verdict: healthy` instead of `degraded`. Same for tempdir-overridden installs (the v0.15.1 dogfood gate's actual scenario) and CI runs. 4 new tests in `TestDoctorPrimitivesMultiSource` (install_dir detection, prefix detection, missing-everywhere, install_dir-precedence-over-prefix). agent-plus-meta unittest: 263 → 267. **Total framework: 405 → 409** (267 + 15 install.sh + 127 skill-plus). Cross-platform: pathlib + os.environ.get throughout; existing shutil.which semantics preserved as the first detection path. Direct push to public main + tag + GitHub Release published, no PR. |
| v0.15.1 | `333df4a` | 2026-04-30 | **CRITICAL P0 HOTFIX: install.sh tarball packaging + doc-drift CI gate + lifecycle parity.** The dogfood gate sub-agent caught what every unit test missed: install.sh on public main was downloading single per-plugin entrypoint bins via `curl` but SKIPPING each plugin's `bin/_subcommands/` directory (introduced v0.12.0, expanded v0.13.5 + v0.14.0 + v0.15.0) AND each plugin's `.claude-plugin/plugin.json` manifest. Every fresh `curl|sh` install produced bins that immediately ImportError'd on first invocation: `from _subcommands import init` had nothing to import. v0.12.0 wizard, v0.13.5 upgrade-check + upgrade, v0.14.0 globalize/localize/where/team-sync/collisions, v0.15.0 uninstall — all broken end-to-end on fresh installs. Unit tests passed because they ran against the source tree, never the install boundary. The exact integration-gap the outside-opinion review warned about. **Fix**: rewrote install.sh to use a tarball-based install layout. Downloads `https://github.com/osouthgate/agent-plus/archive/refs/tags/v<X.Y.Z>.tar.gz` (or `main.tar.gz` fallback), extracts to temp, copies each plugin's full tree (`bin/` + `bin/_subcommands/` + `.claude-plugin/` + `migrations/` + `skills/` + `docs/`) to `$AGENT_PLUS_PREFIX/<plugin>/` (default `~/.local/share/agent-plus/<plugin>/`), then generates a thin wrapper shim at `$AGENT_PLUS_INSTALL_DIR/<plugin>` that does `exec python3 $PREFIX/<plugin>/bin/<plugin> "$@"`. Wrapper goes on PATH; real bin + `_subcommands/` tree live under `$PREFIX`. Existing `sys.path.insert(0, _HERE)` resolves correctly relative to the real bin location, NOT the wrapper. Zero code changes to plugin bins required — install layout absorbs the structural fix. New `--source-dir=PATH` test-only flag bypasses tarball download for hermetic round-trip tests. **Lifecycle ring parity**: added `--auto` flag to `agent-plus-meta uninstall` (per outside-opinion review — init + upgrade both have it; uninstall didn't, breaking the "consistent flag idiom" claim). `--purge` still requires literal `PURGE` typed even under `--auto` (T6 hard-stop). uninstall now removes `$PREFIX/<plugin>/` tree alongside the wrapper — new `kind: primitive_tree` in the JSON envelope schema (additive, non-breaking). New `--prefix PATH` flag mirroring `--install-dir`. **Doc-drift fixes** (5, all from fresh-eyes outside-opinion): README badges `0.11.1→0.15.1`, `tests-377→405`; homeless-pivot mention in "What happens when you install"; new "Versioning" subsection explaining umbrella `VERSION` (tag-bound) vs per-plugin `plugin.json#version` (independent semver); agent-plus-meta/README.md "future v0.13.0 agent-plus-installer skill" → past tense pointing to shipped skill; envelope-example clarification (the schema-lock literal `0.12.0` is illustrative, runtime `tool.version` reflects current build). **Unfair-advantage move shipped**: `.github/scripts/doc-drift-check.py` + `.github/workflows/doc-drift.yml` — stdlib-only Python script asserts `cat VERSION` matches latest tag, README badges match real values, plugin.json#version is valid semver, CHANGELOG entries dated within last 7 days. Runs on PR + push to main. Eat-your-own-cooking: doc-drift is the load-bearing weakness of a contract-driven framework, so gate it. **Dogfooded end-to-end** in a Windows tempdir round-trip — wrapper exec resolves, `_subcommands/` import succeeds, `--version` reports real value (not `unknown`), uninstall cleans both tree and wrapper. 6 new tests (3 install.sh tarball/source-dir/wrapper round-trip + 3 uninstall --auto flag and primitive_tree kind). 263 agent-plus-meta unittest + 15 install.sh + 127 skill-plus = **405 passing**. Direct push to public main + tag, no PR. |
| v0.15.0 | `3c86453` | 2026-04-30 | **agent-plus-meta uninstall + install.sh --uninstall (safe-by-default off-ramp).** Closes the lifecycle ring: install (v0.12.0 wizard) → upgrade (v0.13.5 drift detection + .bak rollback) → uninstall (v0.15.0). Same envelope + flag idiom across all three. Canonical impl at `agent-plus-meta/bin/_subcommands/uninstall.py`. Safe-by-default: no flags → only 5 framework primitive bins removed; workspace, marketplaces, plugins-installed-via-claude-plugin, session history, user-authored skills all KEPT and listed with hints. Escalation flags: `--workspace` (also removes `<repo>/.agent-plus/` AND `~/.agent-plus/`), `--marketplaces` (unregisters marketplace state under `~/.agent-plus/marketplaces/` — plugins INSTALLED from those marketplaces NOT touched, Claude Code owns them), `--all` (bins+workspace+marketplaces; "all of ours" semantics per T2 — does NOT include `--purge`), `--purge` (--all PLUS any agent-plus state we own; NEVER touches user-authored skills, sessions, or installed plugins; ALWAYS requires literal `PURGE` typed even under `--non-interactive` per T6 — the one-way door always requires friction). `--dry-run` prints manifest+envelope changes nothing; `--non-interactive` skips y/N on default scope but does NOT bypass --purge confirmation; `--json` emits envelope; `--install-dir` overrides `$INSTALL_DIR`. install.sh --uninstall: delegates to bin when reachable, self-contained POSIX shell fallback (5-bin removal) when broken/missing — refuses expanded scopes in fallback with exit 3 + re-install hint (T1). JSON envelope schema locked as v0.15.0 public contract at `agent-plus-meta/docs/uninstall-envelope.md`. `kind` enum reserves slots for v0.16+ (`settings_hook`, `daemon_pid`, `migration_state`) without emit today — additive expansion non-breaking. Plugin-cache LIST-only: walks `~/.claude/plugins/cache/` for `@agent-plus` plugins, surfaces `claude_plugin_hints[]` envelope field with exact `claude plugin uninstall <name>@agent-plus` strings; never deletes (Claude Code owns). Self-delete on Windows: detected, emits status:error with manual-removal note for self; other 4 bins succeed normally. POSIX self-delete works as expected (kernel keeps file handle across unlink). **Bonus fix: install.sh verb-dispatcher arg-forwarding bug** (introduced silently in v0.13.0) — `case "$VERB" in upgrade) dispatch_upgrade ;; uninstall) dispatch_uninstall ;; esac` was dropping all args after the verb because the case-statement branches called dispatchers without `"$@"`. Fixed both. v0.13.5's `install.sh --upgrade --dry-run` also benefits (was previously silently dropping --dry-run). The new `--uninstall --dry-run` test surfaced this immediately. T3 deferred (`agent-plus-uninstaller` SKILL.md to v0.16+ — needs v0.13.0 installer skill dogfood data first). Cuts: --reinstall convenience, PATH cleanup helper, `--include-claude-plugins` inside --purge — all deferred. Plugin version bumped 0.13.5 → **0.15.0** (skipping 0.14.0 since that was a skill-plus-only slice, didn't bump agent-plus-meta). VERSION file at repo root bumped to 0.15.0 to match. 14 new tests in `test_uninstall.py` + 3 new in `test_install_script.py` (lead's range was 12-15; impl agent skipped the low-value `unknown_flag_exits_2`/`paths_ordered_by_scope`/`self_delete_on_linux`/`telemetry_event` tests with documented justifications). Total: **399 passing** (260 agent-plus-meta unittest + 12 install.sh + 127 skill-plus). Reviewed at 9.5/10 PASS. Direct push to public main + tag, no PR. |
| v0.14.0 | `7c50dcd` | 2026-04-30 | **skill-plus scope topology — five subcommands for moving skills between scopes.** `globalize <name>` (project → global move; `--keep-local` copies), `localize <name>` (symmetric mirror), `where <name>` (read-only three-tier resolver: project + global + `~/.claude/plugins/cache/`, reports Claude Code's documented loader preference `project > global > plugin`), `team-sync <name>` (localize alias + emitted `commit_hint` — does NOT invoke git), `collisions` (detector + 4 UX modes: interactive prompt / non-tty bail with `verdict: needs_user_input` + `suggested_renames[]` / explicit `--rename name:scope:new-name` (or `name=scope:new-name` defensive parsing) / deterministic `--auto` with project-wins + global-gets-`-global`-suffix per T3). Per-subcommand JSON envelopes locked as v0.3.0 public contract. T1 (interactive + non-tty bail), T2 (three-tier walk including plugin-cache via `rglob` over `~/.claude/plugins/cache/`), T3 (project wins, global gets `-global` suffix on `--auto`) all implemented per locked plan. Lead correction: plugin version 0.2.0 → **0.3.0** (autoplan proposed 0.4.0 skipping a slot — corrected to 0.3.0 since this is additive minor). Cross-platform: `shutil.move`/`shutil.copytree` for cross-volume safety, pathlib everywhere, utf-8 file I/O. Stdlib-only Python. No `~/.claude/` writes anywhere in tests (monkeypatched HOME/USERPROFILE per fixture). 40 new tests across 5 new test files (`test_globalize.py` 8 + `test_localize.py` 8 + `test_where.py` 7 + `test_team_sync.py` 6 + `test_collisions.py` 11 — flagged: 11 instead of 10 because `sys.stdin.isatty` cannot be reliably faked across platforms without a pty, swapped a tty mock for a `--rename`-driven dispatch test). 127 skill-plus tests + 246 agent-plus-meta unittest = **373 total passing**. Reviewed at 9/10 PASS. Two LOW findings deferred (cosmetic): `globalize.run` vs `localize.run` rc inconsistency; `rglob` over plugins cache may be slow on huge caches but correctly guarded. Direct push to public main + tag, no PR. |
| v0.13.5 | `67227dd` | 2026-04-30 | **agent-plus upgrade slice — drift detection, .bak rollback, migrations runner.** Closes the framework's biggest current craft gap. Three pieces: (1) `agent-plus-meta upgrade-check` — cached probe at `agent-plus-meta/bin/_subcommands/upgrade_check.py` with 60min/720min cache TTL, snooze ladder (24h→48h→7d→never, reset on new version), fail-silent 3s `urllib.request` probe of single-root `VERSION` file, frozen JSON envelope public contract; (2) `agent-plus-meta upgrade` — at `agent-plus-meta/bin/_subcommands/upgrade.py`, install-type detector (global / git_local — vendored CUT per /review C4), 5-bin `.bak`-snapshot replacement, migration runner reading `agent-plus-meta/migrations/v*.py` modules with idempotent `migrations.json` history, post-test in-process `cmd_doctor()` gate with automatic rollback on broken-verdict, 4-option `AskUserQuestion` (Yes / Always / Snooze / Never ask). T5 corrected at /review: `--auto` + minor/major bump degrades to snooze (noop) — pre-1.0 safety, refuses to silently land potentially-breaking bumps in CI; (3) `agent-plus-meta/migrations/` — empty-on-day-one runner contract documented in `agent-plus-meta/migrations/README.md`. Plus: `install.sh --upgrade` shim wired to delegate via `exec` (the v0.13.0 verb-dispatcher refactor made this clean); `VERSION` file at repo root (T1: tag-bound, NOT max plugin.json — per-plugin versions remain independent); `.github/workflows/ci.yml` VERSION-vs-tag CI gate on tag pushes (promoted from "reviewer concern" to in-scope per outside-opinion review). 4 stable error codes — `upgrade_user_declined` CUT per /review C2; `--sentinel` mode CUT per outside-opinion review; `silent_upgrade_policy` knob CUT (hardcode patch-only); `telemetry` envelope field CUT (`--no-telemetry` is no-op forward-compat); GitHub API VERSION fallback CUT. 38 new tests (17 upgrade_check + 16 upgrade + 5 migrations). 342 passing total. TTHW gates met: cache-hit <1s, network probe <5s, dry-run <3s, rollback <2s. Reviewed at 9/10 PASS, two P3 fixes auto-applied (T5 auto-degradation correction + dead snooze-validation branch removed). Direct push to public main + tag, no PR. |
| v0.13.0 | `9c5b1ad` | 2026-04-30 | **agent-plus-installer SKILL.md + install.sh dispatcher refactor.** Pure-markdown skill at `agent-plus-meta/skills/agent-plus-installer/SKILL.md` (nested per repo convention; the wizard plan's earlier "top-level" sketch predated this). Five `when_to_use` trigger cues, each gated by a `command -v agent-plus-meta` probe AND a session-decline flag (`AGENT_PLUS_INSTALL_DECLINED`). Killer command is the single `curl … | sh -s -- --unattended` line; the install.sh → `agent-plus-meta init --non-interactive --auto` chain is documented under Architecture. Four explicit do-NOT-use guards (already-installed, declined-this-session, false-positive trigger, sandbox/CI). Five safety rules (surface-never-auto-execute, per-invocation permission prompt, session-decline, no destructive flags without confirmation, report failures verbatim). `allowed-tools: Bash(curl:*) Bash(sh:*) Bash(agent-plus-meta:*)` — `Bash(install.sh:*)` deferred to a future upgrade-driver skill. install.sh refactored to a generic `--<verb>` dispatcher (~30 lines POSIX shell): VERB defaults to `install` (existing behaviour, byte-for-byte preserved); `--upgrade` and `--uninstall` are recognised verbs that exit 2 with "ships in v0.13.5 / v0.15.0" stub messages. The refactor exists to give v0.13.5 and v0.15.0 a clean plug-in surface — eliminates the only real shared-file conflict between the upcoming slices. Per outside-opinion review of the overnight orchestration queue (APPROVE WITH CHANGES). 5 new tests in `test_installer_skill.py` (frontmatter shape, canonical h2 sections, allowed-tools regex, killer-command URL+flag shape). 9 install.sh tests pass without modification. agent-plus-meta total: 203 → 208. Reviewed at 9.5/10 PASS. Direct push to public main + tag, no PR (autonomous overnight ship). |
| v0.12.0 | `be09900` | 2026-04-30 | **Persona-aware onboarding wizard.** `agent-plus-meta init` is now interactive — detects user state (`has_skills`, `has_claude_projects_history`, `env_vars_ready_count`, `agent_plus_already_init`, `homeless`), routes to one of three first-run branches (NEW / RETURNING / SKILL-AUTHOR) with deterministic priority `skill_author > returning > new`, and adds a homeless pivot for cwd-without-repo-context. Cross-repo discovery walks `~/.claude/projects/` with multi-candidate decoder for Windows (`C--dev-foo`) AND POSIX (`-Users-bob-foo`) path encodings, surfaces top 4 by recency. Manual `[m]` paste-in for repos Claude Code hasn't seen. Doctor finale wrapped in try/except (CRITICAL GAP rescue). `--non-interactive --auto` mode emits a frozen JSON envelope as a public contract — for the v0.13.0 `agent-plus-installer` skill that lets Claude Code drive install on the user's behalf. 8 stable error codes (Tier-1 stderr + Tier-3 envelope). Observability JSONL at `<workspace>/.agent-plus/init.log` (`AGENT_PLUS_INIT_LOG=0` opts out). `install.sh --unattended` and `--no-init` flags + auto-chain into the wizard. Companion: skill-plus 0.2.0 ships `list --include-global` (walks `~/.claude/skills/`, flags name collisions across scopes) — used by the SKILL-AUTHOR branch first-win; collision resolution lands in v0.14.0. 41 new tests (37 wizard + 4 install + 4 skill-plus include-global), 299 passing total. Cross-platform Windows + macOS + Linux verified. Three-lane parallel build + /review at 9/10 bar before tag — caught two P1s (plugin.json version drift, missing `--include-global` flag) and three P2s before ship. PR (no PR — direct push to main per solo-ship convention). |

**Upcoming slices** (not yet shipped):

| Slice | Topic | Notes |
|---|---|---|

(Empty — promotion queue cleared 2026-04-30 with v0.10.0.)

Note: slice 6 (`d9ce987`) is `STAGING.md` + staging banner — **PRIVATE ONLY, never cherry-pick**. Slice 7 (`1758144`) is `plans/todo/2026-04-28-backlog-status.md` — also private (the public repo doesn't track `plans/todo/`).

### Doc follow-ups noted but not yet shipped

(Empty — `core.autocrlf=false` shipped in v0.10.0.)

## What's intentionally NOT in this file

- Strategic / business / personal context — that lives in [`plans/todo/2026-04-24-strategic-direction.md`](./plans/todo/2026-04-24-strategic-direction.md) and its appendices.
- Cross-session memory — that lives in `~/.claude/projects/C--dev-agent-plus/memory/`.
- Per-plugin specifics — those live in each plugin's own `README.md` / `CHANGELOG.md`.

## Why this file exists at all

The agent-plus AGENTS.md and READMEs are user-facing and must be byte-identical between the private staging clone and the public repo. Anything staging-specific has to live in a separate file that gets stripped (or simply never cherry-picked) at promotion time. `STAGING.md` is that file.

The SessionStart hook (`.claude/hooks/agent-plus-context.py`) detects when this repo's remote points at `plans-agent-plus` and surfaces a one-line "STAGING MODE" notice automatically, so coding agents working in this repo get the context without needing to read this file every session. This file is the durable reference; the hook is the runtime nudge.
