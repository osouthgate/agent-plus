# Copy to evals/ollama.env (gitignored) so pytest picks up Ollama without manual `set` each session.
# Process environment wins if the same key is already set.

OLLAMA_CHAT_MODEL=llama3.2
OLLAMA_BASE_URL=http://localhost:11434
