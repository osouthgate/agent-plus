{
  "name": "agent-plus",
  "owner": {
    "name": "Oliver Southgate",
    "url": "https://github.com/osouthgate"
  },
  "metadata": {
    "description": "Make AI agents dramatically cheaper at running your infrastructure. Drop-in Claude Code plugins that collapse slow, multi-step API dances into one fast call — deploys, databases, cloud, billing, logs. Zero install pain: single-file Python, also runs standalone."
  },
  "plugins": [
    {
      "name": "hermes-remote",
      "source": "./hermes-remote",
      "description": "Remote CLI for managing a Hermes Agent deployment — cron jobs, env, status, model, config, and one-shot chat via /v1/chat/completions."
    },
    {
      "name": "coolify-remote",
      "source": "./coolify-remote",
      "description": "Remote CLI for Coolify PaaS — apps, env vars with post-write verification, domains, TLS, deploys with --wait, and app exec via SSH + docker exec."
    },
    {
      "name": "hcloud-remote",
      "source": "./hcloud-remote",
      "description": "Minimal Hetzner Cloud CLI — server list/show/reboot, snapshot create/list, ssh by name. Deliberately narrow (no volumes, networks, or LBs)."
    },
    {
      "name": "openrouter-remote",
      "source": "./openrouter-remote",
      "description": "OpenRouter CLI — balance with cron-friendly --alert-below, per-key usage stats, model catalogue search, per-provider endpoints, and API key management."
    },
    {
      "name": "langfuse",
      "source": "./langfuse",
      "description": "Langfuse CLI — export/import prompts for backup and cross-env migration, smoke-test traces, health checks across multiple named instances."
    },
    {
      "name": "railway-ops",
      "source": "./railway-ops",
      "description": "Read-first Railway CLI wrapper — single-call env overviews (services, deploy status, errors, env var NAMES-only) for fast incident triage."
    },
    {
      "name": "supabase-remote",
      "source": "./supabase-remote",
      "description": "Supabase CLI wrapper — strips the agent-envelope preamble from sql queries, single-call rls-audit across every table, gen-types into one command."
    },
    {
      "name": "vercel-remote",
      "source": "./vercel-remote",
      "description": "Read-first Vercel REST API wrapper — single-call project overviews (deployments with commit metadata, domain verification, env var NAMES-only), name-resolved projects, Deploy Hook triggers with --wait, zero env-value leakage."
    },
    {
      "name": "github-remote",
      "source": "./github-remote",
      "description": "Read-first GitHub REST API wrapper — single-call PR/branch overviews (state + checks + reviews + workflow runs + mergeable), name-resolved PRs/issues/runs by branch or title, run wait with branch-resolution, log fetch with redirect fallback. One write surface (pr comment)."
    },
    {
      "name": "linear-remote",
      "source": "./linear-remote",
      "description": "Read-first Linear GraphQL API wrapper — issues get/list/search/create/update/move/assign, project overviews with state-bucketed issues, name-resolved teams/states/labels/assignees, --from-markdown YAML frontmatter issue creation, zero API-key leakage."
    }
  ]
}
